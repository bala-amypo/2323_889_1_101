spring.application.name=demo
server.port=9001
server.forward-headers-strategy=framework

# --- Database (Updated name to inventory_db) ---
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Swagger ---
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.api-docs.path=/v3/api-docs

# --- JWT Configuration (MUST MATCH JAVA CODE) ---
# The Java code uses @Value("${jwt.secret}"), NOT app.jwt.secret
jwt.secret=InventorySuperSecretKeyForPredictorSystem1234567890
jwt.expiration=3600000